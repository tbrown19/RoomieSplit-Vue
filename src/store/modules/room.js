// initial state
import * as getters from './rooms_getters';
import * as actions from './rooms_actions';

const state = {
    percentOfTotalSpace: 0
};

// actions
const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
};
